# for windows since its a little bitch maintained by little suckers a
# === Core Deep Learning Stack (DirectML) ===
torch-directml==0.2.5.dev240914      # Compatible with PyTorch 2.3.1
torch                         # Required base version for torch-directml
torchvision                 # Matches torch 2.3.x builds

# === Core Scientific Libraries ===
numpy                        # Last 1.x series compatible with torch 2.3.x
pandas                        # Stable for Python 3.10
Pillow                       # Verified working (fixes _imaging import)

# === (Optional) Utilities ===
tqdm                        # For progress bars (if your SRGAN uses it)
matplotlib                    # For visualizing results (optional)
scipy                        # Some GAN utilities rely on it
opencv-python