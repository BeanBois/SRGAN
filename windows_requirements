3 for windows since its a little bitch maintained by little suckers a
# === Core Deep Learning Stack (DirectML) ===
torch-directml==0.2.2.dev240614      # Compatible with PyTorch 2.3.1
torch==2.3.1                         # Required base version for torch-directml
torchvision==0.18.1                  # Matches torch 2.3.x builds

# === Core Scientific Libraries ===
numpy==1.26.4                        # Last 1.x series compatible with torch 2.3.x
pandas==2.2.3                        # Stable for Python 3.10
Pillow==10.2.0                       # Verified working (fixes _imaging import)

# === (Optional) Utilities ===
tqdm>=4.66.0                         # For progress bars (if your SRGAN uses it)
matplotlib>=3.8.0                    # For visualizing results (optional)
scipy>=1.11.0                        # Some GAN utilities rely on it
